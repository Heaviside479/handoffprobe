# Research-candidate roadmap note

The canonical committed roadmap remains `docs/ROADMAP.md`.

Research candidates recorded from external or internal evidence are tracked additively in `docs/ROADMAP_RESEARCH_CANDIDATES.md` so they can remain visible without silently expanding committed phase scope.

The first recorded candidate is model-mediated mutation discovery. It is deliberately non-blocking, does not create a 24th stable attack, does not reserve a new `HP-*` ID and does not authorize a release.
